# GitLab CI Docker Credentials Helper

This credentials helper is meant for use in GitLab CI environments. It reads the
Docker registry credentials from `CI_REGISTRY_USER` and `CI_REGISTRY_PASSWORD`.
